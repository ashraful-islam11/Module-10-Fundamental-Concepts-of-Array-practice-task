// todo:            task-2      topic:  Take a portion of an array (not the whole thing) without changing the original. Log the result.


const numbers= [ 12,33,44,55,66,77,77,88,99,17];

console.log('oreginal array of value',numbers);

const willSlice = numbers.slice( 2, 8);
console.log('after the slice',willSlice);

console.log('original array of value', numbers);